/**
 * Function: appendCharacters
 * @description Return the minimum number of characters that need
 *                  to be appended to the end of s so that t becomes
 *                  a subsequence of s.
 * @param {string} s - Any String
 * @returns {string} t - Any String
 * @example
 *  appendCharacters("coaching", "coding"); // 4
 *  appendCharacters("abcde", "a"); // 0
 */
function appendCharacters(s = "", t = "") {
  return 0;
}

module.exports = { appendCharacters };
